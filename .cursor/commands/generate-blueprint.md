Use the context in the `contexts/` folder as input. Choose or mix templates from `www/templates/` as appropriate for the scenario. Produce a Solutions blueprint document and write it under `output/` in markdown only. Use ReadySET templates as reference for structure and sections, but always output markdown. Follow the project writing principles: plain language, direct, no AI giveaways, conversational tone.

You can add extra context after the command; it is included in the prompt (e.g. `/generate-blueprint for the security review scenario`).
